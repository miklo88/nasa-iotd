# Local setup notes — contain internal IDs, never commit
SETUP_NOTES.md

# macOS
.DS_Store

# Dependencies (restored from package-lock.json via `npm ci`)
node_modules/

# Editor
.vscode/
.idea/

# Env / secrets
.env
.env.local
*.token
secrets/
